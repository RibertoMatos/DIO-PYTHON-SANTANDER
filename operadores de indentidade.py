saldo = 1000
limite = 1000

print(saldo is limite)        # Verifica se saldo e limite são o mesmo objeto na memória
print(saldo is not limite)    # Verifica se saldo e limite são objetos diferentes na memória
